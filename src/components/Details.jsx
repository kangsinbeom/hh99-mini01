const Details = ({ info }) => {
  const { eventName, start, end, date, circleColor } = info;
  console.log(eventName);
  return (
    <div>
      <button>sdf</button>
    </div>
  );
};

export default Details;
